# AI Hallucination Checker V2

A simple Python CLI tool to help identify and analyze potential hallucinations in model-generated text. It supports:

- Identifying statements that lack factual support or appear inconsistent.
- Providing a basic confidence score for each sentence.
- Exporting reports as JSON.

## Usage

```powershell
python -m checker --input "output.txt"
```
